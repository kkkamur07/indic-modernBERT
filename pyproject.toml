[project]
name = "indic-modernbert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "huggingface_hub",
    "hydra-core",
    "indic-nlp-library",
    "loguru",
    "pyarrow",
    "tokenizers",
    "transformers",
]

[tool.uv]
package = false
